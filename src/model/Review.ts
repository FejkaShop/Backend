export class Review {
    constructor(
        id: number,
        productId: number,
        userId: number,
        rating: number,
        comment: string | null,
        createdAt: Date,
        updatedAt: Date
    ) {}
}
