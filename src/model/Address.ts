export class Address {
    constructor(
        id: number,
        userId: number,
        street: string,
        city: string,
        state: string,
        country: string,
        postalCode: string,
        createdAt: Date,
        updatedAt: Date
    ) {}
}
